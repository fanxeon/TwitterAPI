peer_node_count = {{ PEER_NODE_COUNT }}
peer_name_prefix = "{{ PEER_NAME_PREFIX }}"
ins_availability_zone = "{{ INS_AVAILABILITY_ZONE }}"
vol_availability_zone = "{{ VOL_AVAILABILITY_ZONE }}"
keypair = "{{ KEYPAIR }}"
flavor_id = "{{ FLAVOR_ID }}"
domain = "{{ DOMAIN }}"
subdomain = "{{ SUBDOMAIN }}"
dns_ttl = {{ DNS_TTL }}
image_id = "{{ IMAGE_ID }}"
volume_size = "{{ VOLUME_SIZE }}"
